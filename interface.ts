
export interface Entries {
  title: string,
  url: string,
  members: number,
  deadline: string,
  host: string
}

export interface Posts {
  title: string,
  url: string,
  preview: string,
  replies: number,
  datePosted: string,
  author: string
}